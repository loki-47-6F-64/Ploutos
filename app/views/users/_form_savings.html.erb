<% if @current_user.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@current_user.errors.count, "error") %> prohibited
      this post from being saved:</h2>
    <ul>
    <% @current_user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
 <% end %>
<%= form_for @current_user, url: update_savings_path do |f| %>
  <p>
    <%= f.label :savings %>
    <%= f.text_field :savings %>
  </p>
  <p><%= f.submit %></p>
<% end %>
